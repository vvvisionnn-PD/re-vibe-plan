import { Workbench } from "@/components/Workbench";

/**
 * 앞 단계에서 만든 분양형 개발사업 수지 도구.
 * 루트 경로는 오피스 개발사업 사업계획서가 쓰므로 이쪽으로 옮겨 두었다.
 */
export default function FeasibilityPage() {
  return <Workbench />;
}
