@AGENTS.md

<!-- 위 한 줄은 create-next-app 이 생성한 것이다. 지우지 말 것. -->
<!-- AGENTS.md 의 `nextjs-agent-rules` 블록은 `next dev` 가 다시 써 넣는다. 직접 수정하지 말 것. -->

---

# 부동산 개발사업 사업계획서 앱

어떤 사업장이든 조건을 입력하면 수지분석과 사업계획서가 나오게 하는 도구다.

## 전체 로드맵

| 단계 | 범위 | 상태 |
| --- | --- | --- |
| 1 | 입력 폼 · 수지 계산기 · 검산 테스트 | **완료** |
| 2 | 사업대상지 공공데이터 시장분석 (서버에서만 호출) | 미착수 |
| 3 | GitHub CLI 로 올려 Vercel 배포 | 미착수 |

다음 단계 작업을 시작하기 전에 아래 규칙을 먼저 읽을 것.

## 단위 규칙 — 가장 자주 깨지는 약속

내부 계산에서 쓰는 단위는 하나뿐이다. 새 필드를 추가할 때도 반드시 따를 것.

| 대상 | 단위 | 예 |
| --- | --- | --- |
| 금액 | **만원** | `3_025_000` = 302.5억원 |
| 면적 | **㎡** | `5000` = 5,000㎡ |
| 단가 | **만원/평** | `2000` = 평당 2,000만원 |
| 비율 | **백분율 숫자** | `4.6` = 4.6% (0.046 아님) |
| 기간 | **개월** | `42` = 42개월 |

- 금액을 원(₩) 단위로 다루지 말 것. 자릿수가 커져 부동소수 오차가 누적되고, 국내 수지표 관행과도 어긋난다.
- 억원·평 환산은 **표시할 때만** 한다. `src/lib/units.ts` 의 `fmtEok` / `fmtPyeong` 등을 쓰고 컴포넌트에서 직접 나누지 말 것.
- 1평 = `400/121`㎡ 다. `3.3058` 같은 반올림 상수를 새로 정의하지 말 것.
- 비율 필드는 `pct()` 로 배율로 바꿔 쓴다. `rate / 100` 을 인라인으로 쓰지 말 것.

## 계산 엔진은 순수해야 한다

`src/lib/` 아래 계산 모듈(`feasibility.ts`, `analysis.ts`, `audit.ts`, `units.ts`)은 순수 함수만 둔다.

- React 훅·브라우저 API·`fetch`·`Date.now()`·난수 금지. 같은 입력이면 항상 같은 결과여야 한다.
- 입력 객체를 변형하지 말 것. 부분 수정은 `analysis.ts` 의 `withPatch()` 를 쓴다.
- UI 에서 계산식을 다시 쓰지 말 것. 화면에 필요한 값이 없으면 엔진에 결과 필드를 추가한다.
- `NaN`/`Infinity`/음수 방어는 엔진 진입부에서 `safeNumber()` 로 한다. 컴포넌트가 방어를 떠안지 않게 한다.

### 순환참조 항목

본PF 소요액은 `PF원금 → 금융비 → 총사업비 → PF원금` 순환을 갖는다. 금융비가 원금에 선형이므로
닫힌 해 `P = base / (1 − k)` 로 푼다. 반복 대입이 아니라 닫힌 해를 쓰는 이유는 결정적이고 검산 가능하기 때문이다.
`k ≥ 1` 이면 발산이므로 `diverged` 로 표시하고 원금을 0 으로 둔다 — 조용히 이상한 숫자를 내지 말 것.

## 검산을 먼저 깨뜨려라

이 앱의 신뢰성은 검산에서 나온다. 계산식을 바꿀 때는 **항상 셋을 함께** 고친다.

1. `src/lib/feasibility.ts` — 계산식
2. `src/lib/audit.ts` — 그 식이 성립함을 주장하는 항등식
3. `src/lib/__tests__/` — 같은 항등식을 모든 프리셋과 극단 입력에 대해 단언하는 테스트

항등식(`kind: "identity"`)과 개연성 점검(`kind: "sanity"`)을 반드시 구분할 것.

- **항등식**은 정의상 반드시 성립한다. 하나라도 깨지면 입력이 아니라 **코드가 틀린 것**이다.
- **개연성 점검**은 입력값 경고다. 실패해도 엔진 버그가 아니다.
- 부등식(`A ≤ B`)을 등식용 `check()` 로 흉내내지 말 것. `assertThat()` 을 쓴다.
  (등식으로 쓰면 조건이 성립하는데도 실패하는 항목이 생긴다.)

앱의 "검산" 탭과 CI 의 테스트는 **같은 코드**(`auditFeasibility`)를 쓴다. 이 대응을 깨지 말 것.

## 명령어

```bash
npm run verify
```

타입체크 → 린트 → 테스트를 한 번에 돌린다. 작업을 마치기 전에 반드시 통과시킬 것.

| 명령 | 용도 |
| --- | --- |
| `npm run dev` | 개발 서버 (Turbopack) |
| `npm run build` | 프로덕션 빌드 |
| `npm run typecheck` | `tsc --noEmit` |
| `npm run lint` | ESLint |
| `npm run test` | Vitest watch |
| `npm run test:run` | Vitest 1회 실행 |

## Next.js 16 / React 19

`AGENTS.md` 의 경고대로 이 버전은 학습 데이터와 다를 수 있다. 코드를 쓰기 전에
`node_modules/next/dist/docs/` 의 해당 문서를 먼저 읽을 것.

이 저장소에서 이미 확인된 사항:

- 레이아웃 props 는 생성된 전역 타입 `LayoutProps<"/">` 를 쓴다. 직접 타입을 쓰지 말 것.
- `react-hooks/set-state-in-effect` 린트 규칙이 켜져 있다. **effect 안에서 `setState` 를 동기 호출하면 빌드가 막힌다.**
  - 외부 시스템(localStorage 등)을 읽을 때는 `useSyncExternalStore` 를 쓴다 → `src/lib/projectStore.ts` 참고.
  - prop 에서 파생되는 표시 상태는 effect 로 동기화하지 말고 렌더 중에 파생시킨다 → `src/components/fields.tsx` 의 `NumberField` 참고.
- Tailwind v4 다. 설정 파일이 아니라 `src/app/globals.css` 의 `@theme inline` 에서 토큰을 정의한다.
  클래스명을 문자열 보간으로 만들지 말 것 (`text-${align}` 같은 것은 스캔되지 않아 적용되지 않는다).

## (2)단계 — 공공데이터 시장분석

### 인증키 — 2개, 서버에서만

| 환경변수 | 발급처 | 쓰는 곳 |
| --- | --- | --- |
| `DATA_GO_KR_KEY` | 공공데이터포털 (data.go.kr) | 법정동코드 · 실거래가(국토부) · 공시지가 |
| `REB_API_KEY` | 한국부동산원 R-ONE (reb.or.kr) | 임대료 · 공실률 · 순영업소득 · 수익률 |

두 키 모두 아래를 지킨다. 예외 없다.

- **`NEXT_PUBLIC_` 접두사를 붙이지 말 것.** 붙는 순간 클라이언트 번들에 박힌다.
- 키를 읽는 모듈 최상단에 **`import "server-only"`** 를 둔다. 클라이언트 임포트를 빌드 타임에 막는다.
- **읽기만 한다.** 로그·에러 메시지·응답 본문·URL 어디에도 키 값을 담지 말 것.
  외부 호출이 실패하면 상태코드와 사유만 남기고 요청 URL 전체를 찍지 말 것 (쿼리에 키가 들어 있다).
- `.env.local` 에 두고 커밋하지 않는다.

> **[확인 필요]** `REB_API_KEY` 는 data.go.kr 키와 **별개 발급 시스템**으로 보인다.
> R-ONE 활용가이드는 자체 포털(`openApiActKeyPage.do`)에서 인증키를 발급받으라고 안내한다.
> R-ONE 에서 직접 재확인할 것 — 다르면 이대로 두고, 같으면 `REB_API_KEY` 를 `DATA_GO_KR_KEY` 로 통합한다.

### 파라미터 이름이 서비스마다 다르다 (자주 틀리는 지점)

기술문서에서 확인한 값이다. 추측으로 바꾸지 말 것.

| 서비스 | 베이스 URL | 인증키 파라미터 |
| --- | --- | --- |
| R-ONE 부동산통계 | `https://www.reb.or.kr/r-one/openapi/` | `KEY` |
| 법정동코드 | `http://apis.data.go.kr/1741000/StanReginCd/` | `ServiceKey` (대문자 S) |
| 상업업무용 실거래가 | `https://apis.data.go.kr/1613000/RTMSDataSvcNrgTrade/` | `serviceKey` (소문자 s) |

- 법정동코드는 `flag=Y` 가 **필수**다. 빠지면 결과가 비어 온다.
- 실거래가 `LAWD_CD` 는 법정동코드 10자리 중 **앞 5자리**다.
- 응답은 세 서비스 모두 **XML 로 받는다.** R-ONE 은 JSON 도 지원한다고 적혀 있으나 JSON 봉투 구조가
  문서에 없다. 문서에 있는 것만 쓴다.

### 캐시

- 공공데이터 `fetch` 는 **하루 캐시**가 기본이다. (`next: { revalidate: 86400 }`)
- **코드성 데이터는 30일 캐시** — 자주 바뀌지 않는다.
  · 법정동코드 조회
  · R-ONE 지역분류코드(`CLS_ID`) 매핑표
- **[확인 필요]** R-ONE 통계값 자체는 분기 공표라 하루 캐시가 과할 수 있다.
  우선 하루로 시작하고 추후 조정한다.

### 경계

- **브라우저는 지역 이름만 보낸다.** 법정동코드와 `CLS_ID` 확인은 서버에서만 한다.
  클라이언트가 코드를 알아야 할 이유가 없고, 코드 조회에도 키가 필요하다.
- 클라이언트 컴포넌트에서 공공데이터 엔드포인트로 직접 `fetch` 하지 말 것 (키 노출 + CORS).
- 외부 응답은 **신뢰할 수 없는 데이터**다. 스키마 검증 후 도메인 타입으로 변환해 넘긴다.
  응답을 그대로 `사업계획입력` 에 펼쳐 넣지 말 것.

### 실제 응답으로 확인한 사실 (추측 금지 — 여기 적힌 값이 근거다)

기술문서만으로는 알 수 없어 실제 호출로 확정한 것들이다. 바꾸기 전에 다시 호출해 볼 것.

- **오피스 통계표의 지역 구분은 시군구가 아니라 상권/권역이다.** 분류가 3단계로 온다.
  `전국 / 시도` → `시도>권역` (서울>도심·강남·여의도마포·기타) → `시도>권역>상권` (서울>강남>테헤란로).
  서울만 2단계 권역이 있고, 나머지 시도는 곧바로 상권으로 간다. **후보는 73개다.**
  매칭·확정 방식은 아래 "R-ONE 권역 매칭 방식" 절을 따를 것.
- **`DTACYCLE_CD` 가 `"YY,QY"` 처럼 쉼표로 묶여 오기도 한다.** 그대로 넘기면 결과가 빈다.
  하나를 골라 넘길 것 (`주기선택()`).
  - 수익률은 **연(YY)** 을 쓴다. 분기 값은 그 분기만의 수익률(0.6%대)이라 캡레이트와 견줄 수 없다.
- **같은 시점에 항목(ITM)이 여럿이다.** 이름으로 고르고 있으며 `[확인 필요] ITM_ID 고정`이 남아 있다.
  - 순영업소득 통계표: 임대수입(%) · 기타수입(%) · 운영경비(%) · 순영업소득(%) · **순영업소득(천원/㎡)**
  - 수익률 통계표: **소득수익률** · 자본수익률 · 투자수익률 → 캡레이트 대응 후보는 소득수익률
- **임대료·순영업소득의 공표 단위는 `천원/㎡` 다.** 앱 단위(만원/평·월)로 환산해 쓴다.
  `X 천원/㎡ = X × (400/121) ÷ 10 만원/평`
  - 순영업소득 `천원/㎡` 의 기간 기준은 **미확정**이다. 분기 누적으로 **가정하고 ×4 연환산**하되
    화면에 "(추정)" 라벨을 반드시 노출한다 → `lib/market.ts` 의 `순영업소득_연환산배수`,
    근거와 조건은 `lib/marketData.ts` 주석. 확인되는 즉시 둘 다 고칠 것.
- **실거래 `buildingUse` 는 짧은 이름으로 온다.** 업무시설은 **`"업무"`** 다.
  `"업무시설"` 로 거르면 한 건도 남지 않는다 (강남구 1년치 923건 중 업무 174건).
- **법정동코드 API 는 문서 코드표에 없는 `INFO-3`("데이터없음 에러")** 를 돌려준다. 자료 없음으로 처리한다.
  - 시도 이름만으로 조회하면 읍면동까지 섞여 와 시군구가 일부만 잡힌다.
    `"서울특별시 강남구"` 처럼 **전체 이름으로 조회**할 것.

### R-ONE 권역 매칭 방식 (/api/market/office-region)

- `SttsApiTblItm.do(ITM_TAG=분류)` 로 오피스 STATBL_ID 의 상권명 전체 목록을 받는다. **30일 캐시.**
  - **73개다** (2024년3분기~ 오피스 시리즈, 네 통계표 모두 동일). 실측으로 확정했다.
    깊이별로 전국·시도 17 / 권역·비서울 상권 27 / 서울 상권 29.
- 시군구명에서 시·군·구를 뗀 핵심어로 **부분일치 우선순위**를 매겨 후보 3~5개를 자동 추린다.
  정확일치 100 > 접두 90 > 포함 80 > 역포함 70, 같은 시도면 +5 / 다른 시도면 −40.
  - 예: "강남구" → `서울>강남`(105) · `서울>강남>강남대로`(95)
  - "서초구"·"영등포구"처럼 이름이 겹치지 않으면 **추천이 0개다.** 이때는 같은 시도의 권역·상권
    전체 목록(서울 33개)을 드롭다운에 채워 사람이 고른다.
- **최종 확정은 사용자가 드롭다운에서 한다.** 첫 조회는 `cls` 를 넘기지 않아 서버가 자동으로 1순위를
  고르고 응답에 `자동선택: true` 로 표시한다. 사용자가 드롭다운을 바꾸면 그때부터 `cls` 를 넘긴다.
  자동 선택을 "사용자가 확정했다"고 표기하지 말 것.
- 화면에 **상권 구획도 원본 링크**를 띄운다 — 이름만으로는 경계를 알 수 없기 때문이다.
  `한국부동산원_상업용부동산 임대동향조사_전국 17개시도 상권 구획도` (기준일 20241031)
  https://www.data.go.kr/data/15086933/fileData.do
- **[확인 필요] 2024년 3분기 상권 재구획으로 상권명이 예전과 달라졌을 수 있다.**
  반드시 **"2024년3분기~" 시리즈 API 응답의 상권명만** 쓸 것 — 구 시리즈(2022년~) 상권명과 혼용 금지.
  (`지역코드 현황_20220518` 같은 구 자료를 끌어다 섞지 말 것.)
  통계표명에 "2024년" 이 없으면 경고를 띄운다.

### 정규식을 문자열로 조립하지 말 것

`` new RegExp(`<${태그}(?:\s...)>`) `` 처럼 템플릿 리터럴에 정규식 이스케이프를 넣으면
문자열 단계에서 `\s` → `s` 로 뭉개져 조용히 안 맞는 정규식이 된다.
백슬래시가 필요 없는 형태로 쓸 것 — 개행 포함은 `[^]`, 공백 제거는 `.split(" ").join("")`.

### node --test 는 ESM 이다

`lib/` 안에서 **런타임 import 는 `.ts` 확장자를 붙인다.** 확장자가 없으면 `npm test` 가 모듈을 못 찾는다.
(`import type` 은 지워지므로 상관없지만 일관되게 붙인다.)
서버 전용 모듈(`lib/server/*`)은 `node --test` 로 실행되지 않으므로, 검산할 값어치가 있는 로직은
`lib/marketParse.ts` · `lib/market.ts` 처럼 server-only 가 없는 파일에 둔다.

### 빌드

- **빌드 때 API 를 부르지 말 것.** 두 키가 **모두 없어도 `npm run build` 가 통과해야 한다.**
  - 키는 모듈 최상단이 아니라 **요청 시점에** 읽는다. 임포트 시 `throw` 하면 빌드가 깨진다.
  - Route Handler 는 기본적으로 캐시되지 않는다. `dynamic = "force-static"` 을 붙이지 말 것 —
    붙이면 빌드 때 실행되어 키를 요구한다.
- 네트워크는 실패한다. 시장분석이 실패해도 **수지 계산은 계속 동작해야 한다.**
  1단계 기능을 외부 호출에 종속시키지 말 것.

## (3)단계 — 배포

- GitHub CLI(`gh`)로 저장소를 만들고 푸시한 뒤 Vercel 에 연결한다.
- `.env.local` 은 커밋하지 말 것. 배포 환경변수는 Vercel 프로젝트 설정에 넣는다.
- 저장소 생성·푸시·배포는 되돌리기 어렵고 외부에 공개되는 동작이다. **실행 전에 사용자에게 확인을 받을 것.**

## 코드 스타일

- 주석과 UI 문구는 한국어로 쓴다. 도메인 용어(수지분석·분양률·보존등기비 등)는 실무 용어를 그대로 쓴다.
- 주석은 "무엇"이 아니라 **"왜"** 를 적는다. 특히 계산식에는 산출근거를 남긴다.
- 사업계획서 표에는 금액만이 아니라 **산출근거 열**을 함께 낸다. 검토자가 숫자를 되짚을 수 있어야 한다.
- 새 입력 필드를 추가하면: `types.ts` → `defaults.ts`(모든 프리셋) → 엔진 → `InputForm` → `ReportView` 순으로 빠짐없이 반영한다.
