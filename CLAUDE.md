@AGENTS.md

# 프로젝트 규칙 — 대주단 제출용 사업계획서 앱

시행사가 대주단에 낼 사업계획서를 입력값만으로 만드는 앱. 특정 사업장을 전제하지 않는다.
사용자와는 한국어로 소통하고, UI 문구도 한국어로 쓴다.

## 진행 상황
- [x] 입력 폼 · 임시 값 버튼 3개 (앞 장 표 A · B · C 그대로)
- [x] 계산(분양수입 · 기말현금 · DSCR · LTV · 한계선 · 민감도 표 데이터) + 검산 테스트
  - 검산 기준: 샘플 A 기말현금 +380.3억 · DSCR 1.27 · LTV 60.2% · 기말현금 0: 90.5% / 2,183만원 · 상환 한계: 80.6% / 1,934만원. 이 값이 바뀌면 안 된다.
- [x] 목차 1~7 사업계획서 화면 · 민감도 표(초록/빨강 + 자동 문장) · 인쇄 버튼
  - 자동 문장은 "이상이면 상환 가능"이 참이 되도록 한계선을 올림한다 (A: 1,934.2만원 → 1,935만원, 80.59% → 80.6%).
  - 6장 리스크 문장의 숫자는 모두 `riskMetrics` · 한계선에서 나온다. 근거 없는 숫자를 넣지 않는다.
- [x] 표시 규칙: 자료 없음 · 지표 옆 계산식 · 천단위 콤마 · 음수 빨강 · 모바일 1열 · 인쇄 A4 (헤드리스 Edge PDF로 확인)
- [x] 실습 7: 2장 시장 분석 — 사업대상지 기준 공공데이터 (아래 "시장 분석 구조")
- [ ] GitHub CLI(`gh`)로 올리고 Vercel로 배포
- 이전 버전(범용 수지 계산기)은 `_archive/v1/`에 보관 중. 빌드·타입 검사·lint에서 제외되어 있다.

## 구조
- `lib/calc.ts` — 계산 전용 순수 함수 (React · Next · 브라우저 API 의존 금지). **모든 함수에 한국어 주석.**
- `lib/presets.ts` — 임시 값 3개 · `lib/format.ts` — 표시용 포맷 · `lib/region.ts` — 사업대상지 이름 점검
- `components/PlanApp.tsx` — 입력 · 결과 화면
- `lib/calc.test.ts` — 검산. `node --test`로 실행한다. **새 테스트 도구를 설치하지 않는다.**
  - 테스트는 `.ts` 확장자로 import한다 (`tsconfig`의 `allowImportingTsExtensions`).
  - Node 타입 제거(type stripping)로 실행하므로 `enum`, `namespace`, 생성자 매개변수 프로퍼티처럼 지워지지 않는 문법은 쓰지 않는다 (`erasableSyntaxOnly`).
  - `lib/` 안에서는 `@/` 별칭을 쓰지 않는다. 상대경로 + `.ts`를 쓴다.

## 시장 분석 구조 (2장)
- `lib/market.ts` — 응답 해석 · 제외 규칙 · 표 계산 (순수 함수, `lib/market.test.ts`로 검산)
- `lib/server/datagokr.ts` — 키 · fetch · 캐시 (`import "server-only"`). 오류 메시지에 URL을 넣지 않는다(키 포함).
- `lib/queue.ts` — 브라우저 호출 동시 4개 제한
- 라우트 (모두 `area=시도 시군구`, 코드는 서버가 확인):
  - `/api/region?q=` 법정동코드 → 시군구 후보
  - `/api/market/presale?area&ym` · `/api/market/rent?area&ym` — 한 달치
  - `/api/market/subscription?area` — 공급위치 LIKE 지역명 · 최근 3년 · 잔여세대
  - `/api/market/subscription/detail?area&id&pblanc` — 주택형별 분양가 · 경쟁률
- 실제 응답에서 확인한 것 (기술문서와 다름):
  - 분양권 `ownershipGbn`은 분양권이면 **빈 값**, 입주권만 "입" → "입"만 제외한다.
  - 전월세 기술문서에는 해제여부 항목이 없다 → 해제 제외 불가, 표에 명시.
  - 같은 전용면적에 분양가가 다른 주택형(예: 125.8735A · D)이 있다 → 그 거래는 웃돈 계산에서 제외.
- 표마다 지역 · 건수 · 제외 기준 · 조회일을 붙인다. 표는 필요한 호출이 모두 끝나야 그린다.

## 차트 (components/charts/)
- **차트 라이브러리를 설치하지 않는다.** SVG를 직접 그린다.
- 차트마다 컴포넌트 하나. 공통 틀은 `ChartFrame.tsx`(제목 · [표로 보기] 토글 · 자료 없음 · 캡션).
- **차트에서 사업 숫자를 계산하지 않는다.** `lib/calc` · `lib/market` 결과를 받아 그리기만 한다.
  축 눈금 · 좌표 변환은 `lib/chart.ts`(`linearScale` · `niceDomain` · `ticks`)를 쓴다.
- 색은 `ChartFrame`의 `CHART_COLORS`만 쓴다: 계획가 = 강조색 · 시장 = 회색 · 가능 = 초록 · 불가 · 음수 = 빨강.
- 차트 아래에 제목 · 단위 · 건수 · 조회일(공공데이터일 때) · 출처를 적는다. 자료가 없으면 빈 축 대신 "자료 없음".
- SVG는 `viewBox` + `w-full min-w-[520px]`(모바일에서는 가로 스크롤, 글자 크기 유지).
- 만든 것: `MarketMarginChart`(2장 시장 대비 여유) · `CashWaterfallChart`(3장 사업수지 폭포).
- 남은 것: 분양권 웃돈 분포 · 월별 거래 건수 · 청약 경쟁률 · 상환 순서 계단 · 분양률 × 기말현금 곡선 · 민감도 히트맵.

## 입력 · 계산 규칙
- 단위: 금액 **억원**, 평당가만 **만원/평(공급기준)**. 공급면적은 평, 전용면적은 ㎡.
- 사업대상지는 시도 + 시군구 **전체 이름** (예: 서울특별시 / 수원시 영통구). 공공데이터 조회 키가 된다.
- 빈 칸은 0이 아니라 `null`. 처음 화면은 빈 폼이다.
- 계산식 (분양률 r, 평당가 p):
  - 분양수입 = (공급면적 × p ÷ 10,000 + 상가) × r
  - 기말현금 = 분양수입 × 0.992 − 세대수 × r × 0.03 + 기타유입 − 고정비
  - 만기 누적 DSCR = 1 + (기말현금 + 후순위) ÷ (본PF + 본PF 금융비용)
  - LTV = 본PF ÷ 분양수입(r = 1)
  - 한계선(이분법): 기말현금 0 / −후순위가 되는 분양률 · 평당가
  - 상환 가능 = 기말현금 ≥ −후순위 (= DSCR ≥ 1). 민감도 표의 초록/빨강도 이 기준이다.
  - 계획 기준값은 분양률 100% · 입력 평당가. 필요한 입력이 하나라도 비었거나 일부만 채운 주택형이 있으면 null(자료 없음).
- 계산을 추가하거나 바꾸면 **손으로 계산한 기대값 테스트**를 함께 넣는다. 계산 과정은 테스트 주석에 남기고, 계산식을 복사해 기대값을 만들지 않는다.

## 표시 규칙
- 입력이 비어 있으면 해당 장은 "자료 없음".
- 각 지표 옆에 계산식을 보여준다. 천단위 콤마를 넣고, 음수는 빨간색으로 표시한다.
- 모바일에서는 세로 1열, 인쇄하면 A4 문서.
- 색은 `app/globals.css`의 토큰(`bg-card`, `text-muted`, `border-line`, `text-bad` 등)을 쓴다.

## 하지 말 것
- 근거 없는 숫자 채우기 (임시 값도 앞 장 표 그대로만 넣는다)
- 실제 사업장 데이터
- 인증키를 코드 · 문서 · 로그에 기록하기

## 공공데이터 · 비밀키
- 인증키는 `.env.local`의 **`DATA_GO_KR_KEY`** — 서버 코드에서만 쓴다.
  - **`NEXT_PUBLIC_` 금지** · 키를 쓰는 모듈은 `import "server-only"` · 키 값을 읽어 보거나 출력(로그 · 응답 · 오류 메시지 · 문서)하지 않는다.
  - Vercel에는 같은 이름의 환경변수로 넣는다.
- 공공데이터 fetch는 **하루 캐시**(`next: { revalidate: 86400 }`), **법정동코드는 30일 캐시**.
- **브라우저는 지역 이름만 보낸다.** 법정동코드는 서버가 이름으로 확인한다 (브라우저가 코드를 보내지 않는다).
- **빌드 때 API를 부르지 않는다** — 키 없이도 `npm run build`가 통과해야 한다. 키는 요청 처리 중에만 읽는다.
- 주소 · 파라미터는 `api_docs/` 기술문서에 적힌 것만 쓴다.
- 외부 API는 Route Handler · Server Action 또는 `import "server-only"`를 둔 모듈에서만 호출한다.
- 응답을 받지 못하면 시장 분석 장에 "자료 없음 — 공공데이터 확인 필요"로 표시하고, 나머지 계산은 계속한다.

## 작업 마무리 체크
- `npm test` · `npm run typecheck` · `npm run lint` · `npm run build` 모두 통과
- Next 16 코드를 쓰기 전에 `node_modules/next/dist/docs/`에서 해당 가이드를 확인한다 (AGENTS.md).
- 커밋과 푸시는 사용자가 요청할 때만 한다. `.env*`는 커밋하지 않는다.
